spring:
  application:
    name: spring-micrometer-otel
management:
  tracing:
    sampling:
      probability: 1
  otlp:
    tracing:
      transport: http
      endpoint: http://localhost:4318/v1/traces
