---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: dash0timeseriesaggregations.operator.dash0.com
spec:
  group: operator.dash0.com
  names:
    kind: Dash0TimeSeriesAggregation
    listKind: Dash0TimeSeriesAggregationList
    plural: dash0timeseriesaggregations
    singular: dash0timeseriesaggregation
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          Dash0TimeSeriesAggregation is the Schema for the dash0timeseriesaggregations API.
          It defines a single rule that aggregates metric time series.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Dash0TimeSeriesAggregationSpec defines the desired state
              of a time series aggregation rule.
            properties:
              attributeModifications:
                description: Modifications applied to the attributes of the aggregated
                  metric.
                items:
                  description: Dash0TimeSeriesAggregationAttributeModification adds
                    or removes attributes from the aggregated metric.
                  properties:
                    kind:
                      description: Whether the matched attributes are dropped or kept.
                      enum:
                      - drop_attributes
                      - keep_attributes
                      type: string
                    spec:
                      description: The details of the modification.
                      properties:
                        context:
                          description: The telemetry context the modification applies
                            to.
                          enum:
                          - resource
                          - scope
                          - datapoint
                          type: string
                        keyMatcher:
                          description: Matcher applied to the attribute keys the modification
                            affects.
                          properties:
                            operator:
                              description: The match operator.
                              enum:
                              - is
                              - is_not
                              - is_set
                              - is_not_set
                              - is_one_of
                              - is_not_one_of
                              - gt
                              - lt
                              - gte
                              - lte
                              - matches
                              - does_not_match
                              - contains
                              - does_not_contain
                              - starts_with
                              - does_not_start_with
                              - ends_with
                              - does_not_end_with
                              - is_any
                              type: string
                            value:
                              description: Single value for operators that accept
                                one value.
                              type: string
                            values:
                              description: List of values for the is_one_of / is_not_one_of
                                operators.
                              items:
                                type: string
                              type: array
                          required:
                          - operator
                          type: object
                      required:
                      - keyMatcher
                      type: object
                  required:
                  - kind
                  - spec
                  type: object
                type: array
              display:
                description: Display configuration for the rule (e.g. the human-readable
                  name shown in the Dash0 UI).
                properties:
                  name:
                    description: Human-readable name for this rule.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              enabled:
                description: Whether this rule is active.
                type: boolean
              match:
                description: Match selects which metrics this rule applies to.
                properties:
                  metricNameMatcher:
                    description: Matcher applied to the metric name.
                    properties:
                      operator:
                        description: The match operator.
                        enum:
                        - is
                        - is_not
                        - is_set
                        - is_not_set
                        - is_one_of
                        - is_not_one_of
                        - gt
                        - lt
                        - gte
                        - lte
                        - matches
                        - does_not_match
                        - contains
                        - does_not_contain
                        - starts_with
                        - does_not_start_with
                        - ends_with
                        - does_not_end_with
                        - is_any
                        type: string
                      value:
                        description: Single value for operators that accept one value.
                        type: string
                      values:
                        description: List of values for the is_one_of / is_not_one_of
                          operators.
                        items:
                          type: string
                        type: array
                    required:
                    - operator
                    type: object
                  otherFilters:
                    description: |-
                      Additional attribute filters that further restrict which metrics contribute to the aggregation. A metric-name
                      match with no attribute filters is valid, so this may be omitted.
                    items:
                      description: Dash0TimeSeriesAggregationAttributeFilter is a
                        single filter against an attribute key.
                      properties:
                        key:
                          description: The attribute key to match against.
                          minLength: 1
                          type: string
                        operator:
                          description: The match operator.
                          enum:
                          - is
                          - is_not
                          - is_set
                          - is_not_set
                          - is_one_of
                          - is_not_one_of
                          - gt
                          - lt
                          - gte
                          - lte
                          - matches
                          - does_not_match
                          - contains
                          - does_not_contain
                          - starts_with
                          - does_not_start_with
                          - ends_with
                          - does_not_end_with
                          - is_any
                          type: string
                        value:
                          description: Single value for operators that accept one
                            value (is, is_not, contains, matches, ...).
                          type: string
                        values:
                          description: List of values for the is_one_of / is_not_one_of
                            operators.
                          items:
                            type: string
                          type: array
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                required:
                - metricNameMatcher
                type: object
              priority:
                description: |-
                  Precedence when more than one rule matches a metric. Lower values are evaluated first (higher precedence).
                  Defaults to 0. Rules with equal priority are ordered deterministically by creation time, then by id.
                type: integer
              sample:
                description: Sample controls the frequency at which matching metric
                  data points are aggregated.
                properties:
                  delay:
                    description: The amount of time to account for delays in scraping,
                      exporting, and data arrival. Defaults to 20s.
                    pattern: ^(\d+(ms|s|m|h|d|w|M|Q|y))+$
                    type: string
                  interval:
                    description: |-
                      The frequency at which metric data points are aggregated. Set this to the scrape/export interval of your metrics
                      to avoid downsampling, or to a larger value to reduce granularity.
                    pattern: ^(\d+(ms|s|m|h|d|w|M|Q|y))+$
                    type: string
                  staleAfter:
                    description: |-
                      Duration after which a source time series with no new data is dropped from memory. Must be at least delay plus
                      interval (raised to that when a smaller value is configured) and longer than the reporting period of the matched
                      sources. Defaults to 5 times the interval, subject to that same minimum.
                    pattern: ^(\d+(ms|s|m|h|d|w|M|Q|y))+$
                    type: string
                required:
                - interval
                type: object
            required:
            - enabled
            - match
            - sample
            type: object
          status:
            description: Dash0TimeSeriesAggregationStatus defines the observed state
              of a Dash0TimeSeriesAggregation resource.
            properties:
              synchronizationResults:
                items:
                  description: Dash0TimeSeriesAggregationSynchronizationResultPerEndpointAndDataset
                    captures the result of a single sync attempt.
                  properties:
                    dash0ApiEndpoint:
                      type: string
                    dash0Dataset:
                      type: string
                    dash0Id:
                      type: string
                    dash0Origin:
                      type: string
                    httpStatusCode:
                      description: |-
                        HttpStatusCode is the HTTP status code that the Dash0 API returned for the failed synchronization attempt, if the
                        failure was caused by an unexpected HTTP response. It is 0 (absent) for successful synchronizations and for
                        transport-level errors (network errors, timeouts) where no HTTP response was received. It is used to decide
                        whether a failed synchronization should be retried.
                      type: integer
                    synchronizationError:
                      type: string
                    synchronizationStatus:
                      description: |-
                        Dash0ApiResourceSynchronizationStatus describes the result of synchronizing a (non-third-party) Kubernetes resource
                        (e.g. synthetic checks) to the Dash0 API.
                      enum:
                      - successful
                      - partially-successful
                      - failed
                      type: string
                  required:
                  - synchronizationStatus
                  type: object
                type: array
              synchronizationStatus:
                description: |-
                  Dash0ApiResourceSynchronizationStatus describes the result of synchronizing a (non-third-party) Kubernetes resource
                  (e.g. synthetic checks) to the Dash0 API.
                enum:
                - successful
                - partially-successful
                - failed
                type: string
              synchronizedAt:
                format: date-time
                type: string
              validationIssues:
                items:
                  type: string
                type: array
            required:
            - synchronizationResults
            - synchronizationStatus
            - synchronizedAt
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
