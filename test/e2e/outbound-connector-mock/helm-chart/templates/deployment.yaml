# SPDX-FileCopyrightText: Copyright 2026 Dash0 Inc.
# SPDX-License-Identifier: Apache-2.0

apiVersion: apps/v1
kind: Deployment
metadata:
  name: outbound-connector-mock
  labels:
    app: outbound-connector-mock-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: outbound-connector-mock-app
  template:
    metadata:
      labels:
        app: outbound-connector-mock-app
        app.kubernetes.io/name: "outbound-connector-mock"
        app.kubernetes.io/version: "1.0.0"
        app.kubernetes.io/part-of: "dash0-operator-test"
        app.kubernetes.io/instance: "outbound-connector-mock"
    spec:
      containers:
        - name: outbound-connector-mock-app
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - name: grpc
              containerPort: {{ .Values.grpcPort }}
            - name: debug
              containerPort: {{ .Values.debugPort }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          readinessProbe:
            httpGet:
              path: /ready
              port: {{ .Values.debugPort }}
            initialDelaySeconds: 1
            periodSeconds: 1
