suite: test custom resource definition
templates:
  - operator/custom-resource-definition-operator-configuration.yaml
tests:
  - it: custom resource definition should match snapshot
    asserts:
      - matchSnapshot: {}