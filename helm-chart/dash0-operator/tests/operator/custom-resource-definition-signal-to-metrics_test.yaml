suite: test signal-to-metrics CRD
templates:
  - operator/custom-resource-definition-signal-to-metrics.yaml
tests:
  - it: custom resource definition for signal-to-metrics should match snapshot
    asserts:
      - matchSnapshot: {}
