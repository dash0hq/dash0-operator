---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: dash0spamfilters.operator.dash0.com
spec:
  group: operator.dash0.com
  names:
    kind: Dash0SpamFilter
    listKind: Dash0SpamFilterList
    plural: dash0spamfilters
    singular: dash0spamfilter
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Dash0SpamFilter is the Schema for the Dash0SpamFilter API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Dash0SpamFilterSpec defines the desired state of Dash0SpamFilter
            properties:
              contexts:
                description: The signal contexts this spam filter applies to (e.g.,
                  log, span, metric).
                items:
                  type: string
                minItems: 1
                type: array
              filter:
                description: The filter conditions that define which telemetry data
                  to drop.
                items:
                  description: |-
                    Dash0SpamFilterCondition defines a single filter condition for spam filtering,
                    matching the Dash0 API's AttributeFilter schema.
                  properties:
                    key:
                      description: The attribute key to match against.
                      type: string
                    operator:
                      description: The comparison operator (e.g., "is", "is_not",
                        "contains", "starts_with").
                      enum:
                      - is
                      - is_not
                      - is_set
                      - is_not_set
                      - is_one_of
                      - is_not_one_of
                      - gt
                      - lt
                      - gte
                      - lte
                      - matches
                      - does_not_match
                      - contains
                      - does_not_contain
                      - starts_with
                      - does_not_start_with
                      - ends_with
                      - does_not_end_with
                      - is_any
                      type: string
                    value:
                      description: The value to compare against. Optional for operators
                        like "is_set" and "is_not_set".
                      type: string
                  required:
                  - key
                  - operator
                  type: object
                minItems: 1
                type: array
            required:
            - contexts
            - filter
            type: object
          status:
            description: Dash0SpamFilterStatus defines the observed state of Dash0SpamFilter
            properties:
              synchronizationResults:
                items:
                  description: Dash0SpamFilterSynchronizationResultPerEndpointAndDataset
                    defines the synchronization result per endpoint and dataset
                  properties:
                    dash0ApiEndpoint:
                      type: string
                    dash0Dataset:
                      type: string
                    dash0Id:
                      type: string
                    dash0Origin:
                      type: string
                    synchronizationError:
                      type: string
                    synchronizationStatus:
                      description: |-
                        Dash0ApiResourceSynchronizationStatus describes the result of synchronizing a (non-third-party) Kubernetes resource
                        (e.g. synthetic checks) to the Dash0 API.
                      enum:
                      - successful
                      - partially-successful
                      - failed
                      type: string
                  required:
                  - synchronizationStatus
                  type: object
                type: array
              synchronizationStatus:
                description: |-
                  Dash0ApiResourceSynchronizationStatus describes the result of synchronizing a (non-third-party) Kubernetes resource
                  (e.g. synthetic checks) to the Dash0 API.
                enum:
                - successful
                - partially-successful
                - failed
                type: string
              synchronizedAt:
                format: date-time
                type: string
              validationIssues:
                items:
                  type: string
                type: array
            required:
            - synchronizationResults
            - synchronizationStatus
            - synchronizedAt
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
